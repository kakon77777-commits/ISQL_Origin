# P4 source status

This stacked GitHub branch is source-focused. The full archival v0.5.0 ZIP contains the fully integrated runtime, wheel, 103-test suite, native reference-decoder logs and 231-file checksum manifest.

The GitHub source compatibility layer preserves the same P4 BridgePlan / ObservationBundle / BridgeReceipt schemas and exit semantics while delegating P0–P3 commands to the stacked P3 source branch.
